# AA × ECI 聚合程序设计

## 1. 产品目标

打开就是一张可以用来筛选模型的表。数据覆盖优先于匹配完整度，稳定的判断规则优先于无休止的提示。

用户侧只有五列：

**模型 / 配置｜AA｜ECI｜单任务（¥）｜发布**

两个指数量纲不同，名次可比、绝对值不可比；页面不显示名次标注。两榜相对位置差 ≥ 15% 的族在 ECI 数值上加黄底（名次分属不同大小的群体，按全局分位比较，不受筛选影响）。没有介绍卡片、图表、上手向导、置信度标签、逐行解释、风险横幅或图例文字；量纲与口径提示只存在于列头与分歧标记的悬停 tooltip 中，页面默认视图零解释性文字。来源署名、AA 版本和生成时间占用一行；后台处理记录不进入页面。不提供「N 次总成本」等派生计算：单任务成本乘以次数是心算可完成的事，页面不为它保留输入框和列。

## 2. 实现形态

Python 标准库完成采集、CSV/ZIP/JSON 解析、归族和 HTML 构建；SQLite 保存稳定身份与历史；原生 JavaScript 完成前端交互。没有运行中的 API 服务器，没有浏览器端第三方请求，没有前端构建工具链。

```text
AA Free API 全部分页 ────┐
                         ├→ 原始快照 → 规范化 → 模型身份与归族 → 全量校验
Epoch 发布成绩 CSV ─────┤                                  ↓
Epoch model_metadata ────┘                         models.json + models.csv
                                                          ↓
                                                     index.html
```

每日批处理一次。HTML 是最终视图，不是数据库。数据库和原始快照不必与公开网页放在一起。

## 3. “全部模型”的实现定义

范围为：AA 接口所有公开语言模型记录，Epoch ECI 成绩表所有记录，以及 Epoch 元数据中的所有模型组。

```text
显示模型集合 = AA 模型族 ∪ Epoch 模型组
显示配置集合 = AA 返回的所有配置
```

未进入另一榜单不是删除理由。没有成本、没有 AA、没有 ECI、年份早、能力低，也不是删除理由。默认仅按 AA 排序，不按完整性筛掉记录。分页仅控制渲染数量，导出和搜索面对全部数据。

Epoch 已收录但尚无 ECI 的组也保留。Epoch 元数据的全部内部版本附在族上，不为没有测量成本的内部设置生成伪报价。

每轮程序核对四个集合：AA 原始配置 ID、Epoch 模型组、Epoch 成绩原始行、Epoch 元数据原始行。每个元素都必须在输出实体的对应关系中找到；AA 配置不可因去重而消失，也不可因多对多连接而倍增。

## 4. 分层模型

```text
厂商 creator
└── 系列 series
    └── 模型族 / 发布实体 family
        ├── AA 配置 configuration
        └── Epoch 内部版本 epoch_versions
```

“系列”是更大的族，如 GPT、Claude、Gemini、Qwen。先使用这一层，不强行把不同厂商不一致的“世代”和“产品档次”塞进一棵深树；需要时可通过 `series` 映射把 Claude 改按 Opus、Sonnet、Fable 等产品线浏览。

Epoch 的 `model_group` 是归族的重要输入，但内部族 ID 不直接等于 Epoch 行号。模型只在 AA 出现时就获得自己的稳定身份；以后 Epoch 测到它，只是新增关联。

模型名称、日期和上游数字行号都不单独充当主键。数据库使用稳定内部 ID 和来源映射；日期是匹配条件与版本消歧信息。Epoch 拟合时生成的 `m1/m2/...` 一类序号不作为跨快照身份。

## 5. JOIN 规则

连接目标不是两张测量表互相拼接，而是分别找到相同的发布实体。

```text
AA 记录 → family_id ← Epoch 模型组
```

处理顺序：先装载 Epoch 模型组，再处理全部 AA 配置。优先使用维护者明确映射；其次复用持久化来源身份；再使用厂商、规范名称与日期寻找唯一匹配。无法形成唯一匹配的记录立即成为独立族，照常显示。

### 名称标准化

统一大小写、空格和连字符；规范常见厂商别名。只移出明确的 effort 后缀，如 `(high)`、`(max)`、`(non-reasoning)`。配置中的 fallback 等文本保留在配置名称上。

不删除 `Pro`、`Mini`、`Flash`、`Distill`、参数规模、预览版本、模型版本号等身份信息。不能因为字符串很像就把两个发布实体合并。

### 日期处理

优先同日匹配。AA 与 Epoch 对同一发布的日期存在细小差异时，默认允许三天差距，但候选必须唯一。明显不同日期的同名版本分开。Epoch 元数据自身会出现同名但日期相差一两天的行（上游日期噪音），这样的行在容差内并入同一组；超出容差的同名组不通过容差合并。

### 模糊匹配

模糊匹配只生成后台候选列表，不自动创造连接，更不影响模型显示。未匹配并不等于待审核才能上线。

### 后来补上另一个榜单

先前只有 AA、后来出现 Epoch 的同名同日发布，会接入已有身份；首次登记的 ID 和历史快照不重建。维护者可修改来源映射修正个别别名，现有页面无需改代码。

## 6. 指标与代表配置

AA 分数与 AA 的 `cost_per_task.total_cost` 属于同一条配置记录。ECI 属于 Epoch 模型组，再挂到模型族。归族不改变任何分数，也不重新计算 ECI。

```text
人民币单任务成本 = 美元单任务成本 × 构建时汇率（中文界面）
英文界面直接显示 AA 原始美元值，不做换算
```

汇率取自构建配置并在表头 tooltip 注明，页面不提供汇率与任务数输入。AA 与 ECI 的筛选按全局名次表达（AA 取族内最高分的名次，ECI 取族名次），同值同名次；成本仅覆盖 AA 公布了单任务成本的配置，覆盖数写入构建统计。

页面默认族内代表配置：AA 最高；同分选价格更低；仍相同则用稳定配置 ID 排序。若选择“族内最低价”，则先按成本，价格相同再看 AA。

过滤条件始终先作用于配置，再选择代表配置。例如 AA ≥ 45 且单任务 ≤ ¥2，必须由同一条配置同时满足，不允许“族里有人分数够，另一个人价格够”。

没有测到的值为 null，在表中显示破折号；零分和零价格保留为 0。数值缺失始终排在数值排序末尾，升序降序都一样。

ECI 在族行显示，属于整个模型族，不复制为子配置成绩；平铺视图的 ECI 列同样表示该配置所属族的 ECI。更大系列的分组标题不提供平均分或拼出来的虚拟报价。

## 7. 前端交互

默认按模型族、AA 降序；族行固定显示最高分档，档位通过行首箭头展开。AA 与 ECI 名次以浅色小字随数值显示：族行 AA 为该族最高分的族级名次（与「AA 前 N」筛选同口径），族行 ECI 为族名次，档位行为该配置在全部配置中的名次；同值同名次、不受筛选影响。名次固定占位，数值右边缘列内对齐。不设平铺视图、族行档位选择与全部展开按钮；逐配置的完整清单由导出 CSV 承担。厂商与系列的浏览由搜索承担，不设下拉筛选。

搜索查模型、厂商、系列、日期和配置名称。AA 与 ECI 的筛选为可自由输入、可叠加的名次上限（「前 N 名」，留空不限，AA 默认前 100；两框同时填写时取交集），成本为绝对上限，按界面货币解释（中文 ¥＝构建时汇率折算，英文 $＝AA 原始值）。五个列头都可排序，发布日期默认新在前。空态的「清除筛选」只清筛选条件，不重置排序；「重置」回到默认（AA 前 100）。单页 50 / 100 / 200 或全部；导出的是当前筛选结果的全部配置。

初次打开就能选，不需要先回答用途问卷。所有来源记录已经载入 HTML，交互不再请求网络。界面支持中英双语：默认跟随浏览器语言，页头按钮切换，选择随筛选状态一起存入网址与本地存储。

筛选与视图状态保存在网址 hash 与浏览器本地，可用「复制链接」分享；快照超过 3 天未更新时页头时间变色。

## 8. 存储与可复现

`entities` 保存内部身份；`source_links` 保存上游记录到身份的映射；`snapshots` 保存每次完整结果及 AA 版本。

每次采集另存全部原始响应，包括每一页 AA、Epoch 成绩文件、元数据 ZIP。`audit.json` 记录覆盖数量与映射、哈希及相似名称候选。

原始值留在模型配置或族中；人民币费用由显示层计算。每次改变汇率不抓新数据。AA 方法版本属于快照元信息，不能仅靠跨版本分数差生成“能力下降”事件；本版不做历史涨跌模块。

## 9. 更新与发布规则

一次运行必须先完成两源采集，再规范化、连接、全量核对和构建。新 HTML 在临时目录完成后，以单文件原子替换方式发布。

API Key 错误、分页重复、缺页、抓取期间评分版本变化或核心字段无法解析时，本轮退出，旧 HTML 不变。维护者日志记录具体错误；不会在公开页面冒出解释提示，也不会在没有提示的情况下把昨天的某些数字伪装成今天新采集的数据。

正常的新模型、单来源模型、没有价格、普通未匹配项，不属于构建失败，不阻止整站更新。

## 10. 自动化部署

推荐先本地运行一次，再把同一程序放入日调度。仓库附 GitHub Actions 工作流；用户配置 Secret 并启用后才执行，不在交付时远程创建仓库或部署。

工作流先安装依赖、运行单元测试，恢复 `pipeline-state` 分支中的 SQLite，采集并构建，保存新的身份库，最后发布到 GitHub Pages。任务串行，避免两次定时任务同时改身份库。公开仓库下 Actions 与 Pages 均免费且 Actions 无分钟数限制。

## 11. 本版工程验收

包含合成数据单元测试与浏览器测试，覆盖全外连接、配置数量不膨胀、缺失模型保留、分页完成、版本切换、日期消歧、后来补齐另一来源、价格为零、成本与能力同配置筛选、全量 CSV 导出及 HTML 嵌入转义。

测试数据不用于生产页面。交付时未配置 AA Key，联网采集链路尚未实跑；下一次实际操作是配置 Key 后执行 `python3 run.py`，取得真实全量输入并完成首次发布。

## 12. 官方接口与字段依据

AA API 契约、分页、Key 及 Cost per Task：
https://artificialanalysis.ai/data-api/docs

Epoch 模型组聚合规则：
https://epoch.ai/data/eci-documentation/data

Epoch 模型元数据字段与 loader：
https://raw.githubusercontent.com/epoch-research/eci-public/main/src/eci/dataloader.py

Epoch 官方实现与数据包：
https://github.com/epoch-research/eci-public

GitHub Pages 自定义工作流：
https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
